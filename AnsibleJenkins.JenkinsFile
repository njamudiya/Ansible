pipeline {
    agent {
        node{
            label 'AnsibleTarget1'
        }
    }   
 stages {
    stage ('PingTest'){
        steps {
             sh '''#!/bin/bash
             echo ${Workspace}/inventory.yml
             ansible-inventory --list -i "${Workspace}/inventory.yml"
             #ansible-playbook "${Workspace}/playbook-pingtest.yml" -i "${Workspace}/inventory.yml" -c local
             '''
            }
    } 
 }  
}
