pipeline {
    agent {
        node {
            label 'AnsibleTarget1'
        }
    }
}
 options {
          ansiColor('xterm')
      }
}
stages {
    steps {
        ansible-inventory --list -i "${Workspace}/Inventory/Inventory.yml"
        ansible-playbook "${Workspace}/Playbooks/playbook-pingtest.yml" -i "${Workspace}/Inventory/Inventory.yml" -c local
    }
}